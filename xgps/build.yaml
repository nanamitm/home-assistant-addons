# Pin the base image per architecture instead of relying on a multi-arch tag
# resolving to the architecture the runner happens to have.
build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base:3.22
  amd64: ghcr.io/home-assistant/amd64-base:3.22
