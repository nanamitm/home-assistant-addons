# Changelog

## 0.1.1

- Pinned the base image per architecture with a `build.yaml` instead of
  resolving a multi-arch tag at build time.

## 0.1.0

- Initial experimental gpsd and chrony implementation.
- Support gpsd SHM PPS and direct chrony PPS modes.
